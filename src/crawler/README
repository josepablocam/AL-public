We use this to collect the relevant notebooks for our project.

You can create the database as follows:

Run `setup.sh` to make sure you have all necessary third party packages (assuming standard setup otherwise).

You can then run `./create_db.sh` with the number of notebooks to collect.

